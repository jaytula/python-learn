# tuple is similar to a list
coordinates = (4, 5)
print(coordinates[0])
print(coordinates[1])

# tuples are immutable
# This would error
#coordinates[0] = 1

# Use when you want data that can't be changed.
